<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <RelativeLayout
        android:id="@+id/forum_item_top"
        android:background="?attr/selectableItemBackground"
        android:padding="12dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <com.huanchengfly.tieba.widgets.theme.TintImageView
            android:id="@+id/forum_item_top_icon"
            android:layout_centerVertical="true"
            app:tint="@color/default_color_primary"
            app:srcCompat="@drawable/ic_arrow_dropup_circle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/content_top" />
        <com.huanchengfly.tieba.widgets.theme.TintTextView
            android:textStyle="bold"
            android:textSize="14sp"
            android:id="@+id/forum_item_top_title"
            app:tint="@color/default_color_text"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Title"
            android:layout_centerVertical="true"
            android:layout_marginStart="12dp"
            android:layout_toEndOf="@id/forum_item_top_icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </RelativeLayout>
</RelativeLayout>