<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <RelativeLayout
        android:id="@+id/forum_item_top"
        android:background="?attr/selectableItemBackground"
        android:padding="12dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <com.huanchengfly.tieba.post.widgets.theme.TintImageView
            android:id="@+id/forum_item_top_avatar"
            android:layout_centerVertical="true"
            android:layout_width="@dimen/size_avatar_tiny"
            android:layout_height="@dimen/size_avatar_tiny"
            android:contentDescription="@string/content_top" />

        <com.huanchengfly.tieba.post.widgets.theme.TintTextView
            android:textStyle="bold"
            android:textSize="14sp"
            android:id="@+id/forum_item_top_title"
            app:tint="@color/default_color_text"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Title"
            android:layout_centerVertical="true"
            android:layout_marginStart="8dp"
            android:layout_toEndOf="@id/forum_item_top_avatar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </RelativeLayout>
</RelativeLayout>